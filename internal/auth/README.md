# Auth module

This module provides the first shared authentication foundation for the ERP.

## Current behavior
- admin signup via POST /auth/signup
- login via POST /auth/login
- JWT issuance using JWT_SECRET
- token validation middleware
